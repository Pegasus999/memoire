import 'package:flutter/material.dart';

class Constant {
  static Color Background = Color.fromRGBO(227, 227, 227, 1);
  static Color Blue = Color.fromRGBO(0, 119, 181, 1);
}

class Auth {
  String label;
  String value;

  Auth(String this.label, String this.value);
}
